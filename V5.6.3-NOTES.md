# V5.6.3 — manual home keepsake image compatibility

The reported blank image is in the manual NestPhotoDialog keepsake, distinct from private story rewards. Its old exporter converted an HTML postcard through html-to-image / SVG foreignObject. The user's iOS screenshots match the upstream embedded-image omission reports (#348, #361, #488). Physical iOS reproduction was unavailable; this remains an evidence-supported diagnosis, not a claimed device reproduction.

- Compose the existing paper/photo/nickname/caption layout directly from the decoded captured Blob onto Canvas 2D. No DOM/SVG image rasterization or second image fetch remains in this feature.
- Wait for actual image decoding and the existing brand font. Reject empty, undecodable or visually uniform captures rather than offering a blank finished card.
- Preview, download and Web Share use the same finished PNG. Preserve portrait/landscape layout, original uncropped room image, nickname and date, guest fallback, local-only keepsake behavior and absence of stamps.
- Keep the preview container mounted so regeneration or nickname changes cannot leave an export waiting on a removed DOM subtree. Release source URLs on success and failure.
- No database, account data, ASMR, room model or story-pool changes.

Validation: 142 local tests pass; production build passes. Actual browser day/night manual captures with two resident models produced visible 1080×1910 final PNGs; closing and recapturing worked. Save initiated using the final image; no new downloaded filesystem artifact was exposed by the managed browser, so external download completion is not asserted. Room engine reported audio playing while the photo modal was open. No real iPhone/WebKit session was available for physical-device certification.

