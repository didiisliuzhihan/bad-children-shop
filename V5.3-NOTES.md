# V5.3 — 抽取轨道往返修复

抽取按钮现在沿轨道平滑地左右往返；移动与旋转分层处理，不再复用加载环的单向旋转动画。转向采用缓入缓出，按按钮宽度计算行程，适配手机竖屏与横屏。减少动画模式下保持静止，不制造快速闪动。

本次线上更新只涉及按钮 UI。音量平衡、首次触摸启动声音与非阻塞抽取逻辑保持 V5.2 实现。没有新增玩具目录、收藏记录，也没有修改 Supabase 权限。

验证：15 项单元测试、5 种屏幕/动画设置的轨道检查、7 种声音启动场景回归。触摸与音频策略是在桌面 Chrome 中模拟验证，并非物理 iPhone 实测。

构建继续为 JS/CSS 压缩内联的单 HTML 入口；模型、图片与音频仍使用外部资源。全部源文件保留。
