# 打开 Bad Children Shop

在本项目文件夹中，双击 **打开扭蛋商店.cmd**。它会自动启动本机预览，并在默认浏览器打开：

[http://127.0.0.1:4173](http://127.0.0.1:4173)

把这个网址加入浏览器收藏夹即可。只要本机预览仍在运行，就可以随时刷新或重新打开；关闭浏览器不会关闭预览。重启电脑后，再双击一次启动入口即可，无需打开 Codex 或输入命令。

本地预览直接读取项目中的模型、图片、字体和音频。断网也能进入商店、抽扭蛋和保存在当前浏览器；云端连接恢复后会尝试同步收藏。请保留整个项目文件夹，尤其是 `dist`、`assets/delivery` 和 `scripts`。

`C:/…/index.html` 是电脑上的文件路径，不能当作互联网网址。项目根目录的 `index.html` 是 React 开发入口；`dist/index.html` 才是打包后的单 HTML。正式入口现已配置好云端素材，可以联网打开；本地浏览请优先使用上述启动入口，它能直接读取硬盘上的素材。

此网址仅在本电脑上可用，手机和别人的电脑无法访问这个 `127.0.0.1`。对外分享需要另外发布到网站托管服务。发布版与本地预览共用同一份 React 源码；发布时大型资源使用 Supabase Storage URL，不会把模型或音频塞入 HTML。

如果仍然无法打开，先重新双击启动入口。启动错误记录在 `.preview/server-error.log` 中。请固定使用同一浏览器和同一个网址；换浏览器、清除网站数据或切换到 `localhost` 都可能使用不同的匿名收藏身份。

源码、Blender 工程、未压缩模型和原始媒体均保留在项目里。
