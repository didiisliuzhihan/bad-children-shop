# Uncompressed model masters

Editable Blender projects are in `/blender`. These GLBs retain the uncompressed geometry; production uses the compressed files in Supabase Storage. Keep both source and delivery copies when rebuilding.
